doctype html
html
  head
    meta(charset='utf-8')
    title SOUND Roster
    script.
      var $IDENTITY = {
        characterId: 996613635,
        name: 'Bloodstripes',
        role: 'admin',
      };
    style.
      body {
        margin: 0;
        padding: 0;

        font-family: Helvetica, 'Calibri', Arial, sans-serif;
        font-size: 16px;
      }
  body
    #app
      keep-alive
        router-view(
            'v-if'='$route.meta.keepAlive'
            ':identity'='identity'
            )
      router-view(
          'v-if'='!$route.meta.keepAlive'
          ':identity'='identity'
          )
    script(src='/dist/home.build.js')
