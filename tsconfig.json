{
  "extends": "./tsconfig.common.json",

  // This tsconfig exists solely to provide nice code completion for IDEs.
  // See the tsconfigs in /src/server etc for the real versions.

  "files": [],
  "references": [
    { "path": "./src/server" },
    { "path": "./src/client" },
    { "path": "./src/shared" },
  ]
}
