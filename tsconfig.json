{
  "compilerOptions": {
    // Write compiled files to this directory
    "outDir": "./out/server",

    // Node doesn't support all of es2017, but close enough
    "target": "es2017",
    // Export Node-style modules since this is running on Node
    "module": "commonjs",
    // We're running on Node, so use this strategy.
    "moduleResolution": "node",

    // Various strictness guarantees when compiling
    "strict": true,
    "noImplicitReturns": true,
    "alwaysStrict": true,
    "forceConsistentCasingInFileNames": true,

    // According to the TS team, this should always be set to true; the default
    // is false for backwards compatibility.
    "esModuleInterop": true,
    "allowSyntheticDefaultImports": true,

    "lib": ["es2017", "dom"],                             /* Specify library files to be included in the compilation:  */
    "allowJs": true,                       /* Allow javascript files to be compiled. */
    "sourceMap": true,                     /* Generates corresponding '.map' file. */
  },
  "include": [
    "src/**/*"
  ],
  "exclude": [
    "node_modules",
    "static",
    "out/**/*"
  ],
}
